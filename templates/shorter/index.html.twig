{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form, {'attr': {'method': 'post'}}) }}
    {{ form_end(form) }}
<br/>
    {% if shortUrl %}
        {{ shortUrl }}
    {% endif %}
{% endblock %}
